# Pomodoro Timer 実装機能一覧

このドキュメントは、[architecture.md](architecture.md) に基づく実装対象機能を整理したものです。

## 1. 必須機能（フェーズ1: MVP）

### 1.1 タイマー基本操作
- Start
- Pause
- Resume
- Reset

### 1.2 モード切替
- 作業（work）
- 短休憩（shortBreak）
- 長休憩（longBreak）

### 1.3 時間計算ロジック
- `endsAt - now` で残り時間を再計算する
- タブ非アクティブ復帰時の時間ズレを補正する
- 0秒到達時に完了イベントを発火する

### 1.4 状態管理
- `mode`
- `isRunning`
- `startedAt`
- `endsAt`
- `cycleCount`
- `settings`

### 1.5 UI表示
- 残り時間表示（mm:ss）
- 現在モード表示
- 実行状態（実行中/停止中）の視覚化
- ボタン活性/非活性制御

### 1.6 設定機能（ローカル保持）
- 作業時間
- 短休憩時間
- 長休憩時間
- 長休憩間隔（long break every）
- 自動開始設定（休憩/作業）
- ブラウザローカル保存（例: LocalStorage）

### 1.7 レスポンシブ対応
- モバイル表示最適化
- デスクトップ表示最適化

## 2. バックエンド機能（フェーズ2）

### 2.1 Flaskページ配信
- ルートで初期画面を配信する

### 2.2 設定API
- `GET /api/settings`
- `PUT /api/settings`
- 入力バリデーション
- エラーレスポンス形式の統一

### 2.3 セッションAPI
- `POST /api/sessions`（セッション完了記録）
- `GET /api/sessions?from=...&to=...`（期間指定履歴取得）
- クエリパラメータ検証

### 2.4 永続化（SQLite）
- settingsテーブルのCRUD
- sessionsテーブルのCRUD
- Repository抽象（本番: SQLite / テスト: InMemory）

### 2.5 Service層
- タイマー業務ロジック
- セッション記録ロジック
- Flaskハンドラ薄型化（検証・呼び出し・整形のみ）

## 3. 拡張機能（フェーズ3）

### 3.1 通知
- 通知音
- ブラウザ通知

### 3.2 自動遷移強化
- サイクル数に応じた長休憩自動判定
- 自動開始設定の厳密適用

### 3.3 履歴可視化
- 日次/週次の実績表示
- モード別集計

### 3.4 将来拡張
- ユーザー認証
- クラウド同期
- 統計ダッシュボード
- PWA化（オフライン・通知強化）

## 4. 品質・保守性のための実装要件

### 4.1 純粋関数中心のステートマシン
- 入力: 現在状態、イベント、現在時刻
- 出力: 次状態

### 4.2 Clock抽象
- 本番: SystemClock
- テスト: FakeClock

### 4.3 DTO/スキーマ固定
- Request/Response形式の明確化
- 境界値・欠損・不正値検証の機械化

### 4.4 テスト実装
- ユニットテスト（状態遷移、時刻計算、Service）
- API統合テスト（正常系/異常系）
- E2E主導線（Start -> Pause -> Resume -> Complete）

## 5. 実装チェックリスト（最小）

- 3モードで開始・停止・リセットができる
- タブ放置後の復帰でも残り時間が正しい
- 設定が保存され、再読込後も反映される
- 設定APIの取得・更新ができる
- セッション完了記録と期間取得ができる
- 主要テストが通る

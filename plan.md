# Pomodoro Timer 段階的実装計画

この計画は、[architecture.md](architecture.md) と [features.md](features.md) をもとに、段階的に実装を進めるための実行計画です。

## 実装粒度の基準

1. 1ステップは半日〜1.5日で終わるサイズにする
2. 1ステップごとに画面で確認できる結果を作る
3. 1ステップごとに受け入れ条件を3〜5個決める
4. 1ステップごとに最低1つのテスト（または手動検証手順）を追加する
5. 1PRは原則1ステップ分に限定する

## 段階的な実装計画（推奨）

### 1. ステップ0: 土台準備
目的: 後続実装を迷わず進めるための最小骨組みを作る

実装:
- 単一画面表示
- 静的ファイル配信
- 最小レイアウト
- 開発起動手順整備

完了条件:
- 画面が表示され、操作ボタンの見た目だけ先に揃っている

### 2. ステップ1: タイマー中核（純粋関数）
目的: 時間計算と状態遷移をUIから分離する

実装:
- `mode` / `isRunning` / `startedAt` / `endsAt` / `cycleCount` / `settings` を扱う状態遷移
- `endsAt - now` 再計算
- 0秒到達判定

完了条件:
- Start / Pause / Resume / Reset を関数レベルで再現できる

テスト重点:
- 状態遷移
- 境界値（0秒、負値、連打）

### 3. ステップ2: UI連携（MVP操作）
目的: 中核ロジックを画面操作に接続する

実装:
- ボタンイベント
- 残り時間表示
- モード表示
- 活性/非活性制御

完了条件:
- 画面上で Start / Pause / Resume / Reset が通しで動く

### 4. ステップ3: 設定のローカル保持
目的: MVPの実用性を確保する

実装:
- 作業/休憩時間
- 長休憩間隔
- 自動開始設定
- ローカル保存（例: LocalStorage）

完了条件:
- 再読み込み後も設定が復元される

テスト重点:
- 不正値（0、負数、極端な大値）の扱い

### 5. ステップ4: モード遷移とサイクル制御
目的: ポモドーロらしい動きを完成させる

実装:
- work -> shortBreak
- 一定回数で longBreak
- 必要に応じて自動開始

完了条件:
- 連続セッションで期待どおりにモードが遷移する

### 6. ステップ5: Flask API（settings/sessions）接続
目的: フロント単体MVPからサーバ連携へ移行する

実装:
- settings 取得/更新API
- sessions 作成/取得API
- バリデーション
- 統一エラー形式

完了条件:
- 設定保存先をローカルからAPIへ切替できる

テスト重点:
- 正常系
- バリデーション異常系

### 7. ステップ6: SQLite + Repository + Service分離
目的: 保守しやすいバックエンド構造にする

実装:
- Repository抽象
- SQLite実装
- Service集約
- 薄いハンドラ化

完了条件:
- DB永続化が通り、ハンドラに業務ロジックが残っていない

### 8. ステップ7: 履歴表示UI
目的: ユーザー価値を可視化する

実装:
- 期間指定で履歴一覧
- 基本集計（件数/合計時間）

完了条件:
- セッション完了後、画面で履歴確認できる

### 9. ステップ8: 品質強化
目的: 安定運用できる状態にする

実装:
- ユニットテスト拡充
- API統合テスト
- 主要導線E2E
- エラー時UX改善

完了条件:
- 想定主導線の回帰が自動で検出できる

### 10. ステップ9: 拡張機能
目的: 体験向上と将来拡張の入口を作る

実装:
- 通知音
- ブラウザ通知
- 日次/週次可視化

完了条件:
- MVPの動作を壊さず機能追加できる

## 各ステップの受け入れ条件テンプレート

1. 期待操作を3手順で再現できる
2. 異常入力1〜2ケースで破綻しない
3. 既存主要機能の回帰がない
4. 最低1つのテストが追加されている
5. 次ステップに必要なインターフェースが固定されている

## 進め方のコツ

1. 先にフロントだけで完成形の挙動を作る
2. その後にAPI/DBへ差し替える
3. 通知・統計は最後に回す
4. 各ステップ完了ごとに [features.md](features.md) を更新する
